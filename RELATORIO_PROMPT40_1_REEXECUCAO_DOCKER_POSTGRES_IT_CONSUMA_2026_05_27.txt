RELATÓRIO — PROMPT 40.1
REEXECUÇÃO DOCKER LOCAL + POSTGRESQL REAL + TESTCONTAINERS + VALIDAÇÃO END-TO-END DA CONSUMA
Data: 2026-05-27
Timezone do ambiente: Africa/Luanda

=====================================================================
ADENDO (ATUALIZAÇÃO NESTA MESMA DATA — DOCKER FUNCIONAL)
=====================================================================
Após confirmação do utilizador, foi possível acessar o Docker daemon e executar containers (via execução com permissão escalada fora do sandbox).

Status atual da validação 40.1: AINDA NÃO CONCLUÍDA.

Motivo (novo bloqueio técnico):
- Flyway em PostgreSQL limpo NÃO completa até o fim (V62) devido a erros em migrations (principalmente:
  - uso de `ADD CONSTRAINT IF NOT EXISTS` (não suportado em PostgreSQL),
  - updates com referência inválida ao alias do target table dentro de JOINs no FROM em UPDATE,
  - baseline V1 já contém estruturas criadas por migrations subsequentes (exigindo idempotência consistente).
)

Medidas tomadas (parciais, para destravar migração em banco limpo):
- Criado `docker-compose.local.yml` com Postgres + API (porta 8080) e datasource apontando para o serviço postgres.
- Ajustado `docker-compose.local.yml` para desabilitar `SPRING_JPA_DEFER_DATASOURCE_INITIALIZATION` e eliminar ciclo Flyway/JPA observado no container.
- Aplicadas correções em múltiplas migrations para:
  - `CREATE TABLE/INDEX` com `IF NOT EXISTS` quando aplicável;
  - substituir `ADD CONSTRAINT IF NOT EXISTS` por blocos `DO $$ ... $$` com checagem em `pg_constraint`;
  - reescrever UPDATEs problemáticos (ex.: V7) usando CTE para evitar referência inválida do alias do target table.

Mesmo após essas correções parciais, a migração ainda parou na cadeia 41→42 (V42 falhava por `ADD CONSTRAINT IF NOT EXISTS`), e existem outras migrations posteriores com o mesmo padrão que ainda precisam ser corrigidas antes de validar V1..V62 end-to-end.

Conclusão do adendo:
- Docker/Postgres em container: OK.
- Flyway em banco limpo (V1..V62): NÃO OK (ainda falha).
- Healthcheck, Testcontainers/ITs e fluxos HTTP+DB: NÃO executados (dependem do Flyway completar e API subir).

=====================================================================
1. RESUMO EXECUTIVO
=====================================================================
Status da validação 40.1: BLOQUEADA (BLOQUEANTE DE AMBIENTE)

Motivo do bloqueio:
- Docker daemon NÃO acessível pelo utilizador atual.
- Erro reproduzido: "permission denied while trying to connect to the docker API at unix:///var/run/docker.sock"

Impacto:
- Não foi possível executar docker compose local, PostgreSQL real em container, Flyway em banco limpo, Testcontainers/ITs com containers, nem fluxos E2E via HTTP+DB real.

Decisão (forçada por bloqueio):
- REPROVADO (apenas no sentido de NÃO VALIDADO para a fase 40.1).
  Observação: o código pode estar OK, mas a validação obrigatória não pôde ser concluída neste ambiente.

=====================================================================
2. AMBIENTE USADO
=====================================================================
- CWD: /home/centraltec_admin/Documentos/Eng. Margarida/Sistema de Restauração
- Utilizador: centraltec_admin (uid=1000, gid=1000)
- Grupos: centraltec_admin, nogroup

=====================================================================
3. DOCKER DAEMON STATUS
=====================================================================
Comandos executados e resultados:

3.1) docker version
- Docker client: OK (Docker Engine - Community 29.4.1)
- Resultado: FALHA ao conectar no daemon
  Erro: permission denied while trying to connect to the docker API at unix:///var/run/docker.sock

3.2) docker info
- Resultado: FALHA ao conectar no daemon
  Erro: permission denied while trying to connect to the docker API at unix:///var/run/docker.sock

3.3) docker compose version
- Resultado: OK (Docker Compose v5.1.3)

Evidência do socket:
- /var/run/docker.sock => srw-rw---- 1 nobody nogroup ...
- O utilizador está no grupo nogroup, mas ainda assim o acesso ao daemon falha (necessita investigação/ajuste no host/daemon).

Conclusão:
- BLOQUEANTE. Conforme regra do prompt, a validação não pode continuar “simulada”.

=====================================================================
4. BRANCH E COMMIT
=====================================================================
- Branch atual: restauracao-antes-da-loja
- HEAD (git log --oneline -5):
  - 958df50 fase 49 concluida
  - 1b55506 Prompt 47 — Concluído e aprovado
  - 23c8c5d Prompt 48 — Concluído e aprovado
  - bfe2864 Prompt 46 — Concluído e aprovado
  - a5f1fa7 fase 44.2 okay

=====================================================================
5. ALTERAÇÕES LOCAIS
=====================================================================
git status (resumo):
- Modificados:
  - src/main/java/com/restaurante/delivery/service/DeliveryCourierMatchingService.java
  - src/main/java/com/restaurante/model/entity/DeliveryJob.java
  - src/main/java/com/restaurante/model/enums/OperationalEntityType.java
  - src/main/java/com/restaurante/model/enums/OperationalEventType.java
  - src/test/java/com/restaurante/delivery/service/DeliveryCourierMatchingServiceTest.java

- Não rastreados (novos arquivos):
  - Vários arquivos em src/main/java/... (delivery economics) incluindo:
    - src/main/resources/db/migration/V62__delivery_economics.sql
    - src/test/java/com/restaurante/delivery/DeliveryEconomicsFlowIT.java
    - múltiplos entities/enums/repositories/services relacionados a courier/settlement/fees

Observação importante:
- No histórico do Prompt 40 foi citado que existiam alterações locais pendentes em `pom.xml`, `application.properties`, `src/test/resources/application.properties`, `docker-compose.local.yml`.
- Neste ambiente, `pom.xml` não aparece como modificado em `git status`, e `docker-compose.local.yml` NÃO existe na raiz do repo.

=====================================================================
6. ARQUIVOS DOCKER USADOS / VERIFICADOS
=====================================================================
- docker-compose.local.yml: NÃO ENCONTRADO (arquivo ausente)
- docker-compose.yml: existe, mas contém apenas a API (sem Postgres)
- docker-compose.prod.yml: existe e contém Postgres + API (perfil prod, exige variáveis/segredos)
- Dockerfile: existe e empacota `target/sistema-restauracao-1.0.0.jar`

Nota sobre context-path:
- src/main/resources/application.properties define `server.servlet.context-path=/api`
- O endpoint esperado pelo prompt: GET http://localhost:8080/api/actuator/health (em compose atual a API expõe 8081:8080)

=====================================================================
7. CONTAINERS INICIADOS / docker compose ps
=====================================================================
- NÃO EXECUTADO (bloqueado por falta de acesso ao Docker daemon)

=====================================================================
8. LOGS RELEVANTES DA API
=====================================================================
- NÃO EXECUTADO (bloqueado por falta de acesso ao Docker daemon)

=====================================================================
9. RESULTADO DO HEALTHCHECK
=====================================================================
- NÃO EXECUTADO (bloqueado por falta de acesso ao Docker daemon)

=====================================================================
10. RESULTADO FLYWAY EM POSTGRESQL REAL
=====================================================================
- NÃO EXECUTADO (bloqueado por falta de acesso ao Docker daemon)

=====================================================================
11. RESULTADO DOS TESTES UNITÁRIOS
=====================================================================
- NÃO EXECUTADO neste prompt 40.1 (o objetivo era reexecução com Docker + Testcontainers).
  Nota: no Prompt 40 anterior foi reportado `mvn test` OK, porém aqui não foi revalidado.

=====================================================================
12. RESULTADO DOS TESTES DE INTEGRAÇÃO / TESTCONTAINERS (*IT)
=====================================================================
- NÃO EXECUTADO (bloqueado por falta de acesso ao Docker daemon)

=====================================================================
13. TESTES MANUAIS E2E (HTTP + DB REAL)
=====================================================================
- NÃO EXECUTADO (bloqueado por falta de Docker + Postgres real em container)

=====================================================================
14. ERROS ENCONTRADOS
=====================================================================
1) BLOQUEANTE: Docker daemon inacessível
   - permission denied ao conectar em /var/run/docker.sock

2) Bloqueio adicional (mesmo com Docker funcional):
   - `docker-compose.local.yml` ausente e `docker-compose.yml` não define Postgres
   - Para cumprir o Prompt 40.1 (Postgres real + API em container + banco limpo), é necessário um compose local com Postgres + API e um plano claro de variáveis para dev/local.

=====================================================================
15. CORREÇÕES APLICADAS
=====================================================================
- Nenhuma alteração aplicada neste prompt 40.1 (não foi solicitado corrigir código; validação bloqueada por ambiente).

=====================================================================
16. PENDÊNCIAS / AÇÕES RECOMENDADAS (PRÓXIMO PASSO)
=====================================================================
Para destravar e reexecutar o Prompt 40.1 de forma real:

A) Corrigir acesso ao Docker daemon (host):
   - Confirmar que o serviço/daemon Docker está a correr e que o utilizador consegue executar `docker ps` sem erro.
   - Verificar grupo/ACLs corretos (normalmente socket `root:docker` e utilizador no grupo `docker`) OU ajustar configuração do daemon/rootless conforme o setup do host.
   - Após ajuste, reabrir sessão (logout/login) se houve mudança de grupos.

B) Definir compose local (repo):
   - Criar/recuperar `docker-compose.local.yml` com, no mínimo:
     - Postgres (volume dedicado para limpeza com `down -v`)
     - API (perfil dev/local) apontando para o Postgres do compose
   - Garantir que o endpoint `GET http://localhost:8080/api/actuator/health` (ou mapear 8080 externamente) está consistente com o prompt.

C) Reexecutar a sequência completa:
   - `docker compose -f docker-compose.local.yml down -v`
   - `docker compose -f docker-compose.local.yml up -d --build`
   - `curl -i http://localhost:8080/api/actuator/health`
   - Validar Flyway em banco limpo
   - `mvn -Dmaven.repo.local=/tmp/m2 test`
   - `mvn -Dmaven.repo.local=/tmp/m2 verify` (ou equivalente para Failsafe/*IT)
   - Executar os fluxos HTTP de preview/provision (CONSUMA_PONTO_V1 / CONSUMA_REST_V1) + validação no DB real

=====================================================================
17. RISCOS RESTANTES
=====================================================================
- Sem Docker funcional, não há garantias de:
  - compatibilidade real de profiles/env vars em container,
  - migrações Flyway em banco limpo real,
  - execução efetiva de Testcontainers e suites *IT,
  - comportamento end-to-end de templates e fluxos operacionais em stack containerizada.

=====================================================================
18. DECISÃO FINAL (PROMPT 40.1)
=====================================================================
REPROVADO (BLOQUEADO POR AMBIENTE — validação obrigatória não executada).

=====================================================================
19. PRÓXIMO PROMPT RECOMENDADO
=====================================================================
PROMPT 40.1 (REEXECUÇÃO) — repetir integralmente após:
1) corrigir permissão/acesso ao Docker daemon;
2) garantir existência de `docker-compose.local.yml` com Postgres + API.
