CONSUMA Backend — Prompt 0.2 (Baseline Real do Schema Atual com Flyway)
Data: 2026-05-14 (Africa/Luanda)

1) RESUMO EXECUTIVO
- Substituído o `V1__baseline_schema.sql` marker (vazio) por um baseline SQL REAL do schema atual.
- O baseline V1 agora contém: CREATE TABLE, PKs, FKs, UNIQUEs, indexes e constraints (PostgreSQL-like).
- Preparada validação em PostgreSQL real via Testcontainers (profile `it-postgres`) e estabilizado o unit test profile (H2) sem Flyway.
- Ajustadas configurações para orientar a linha de evolução “Flyway cria / Hibernate valida” em ambientes controlados.
- Nenhuma entidade de domínio foi alterada. Nenhuma regra de negócio foi alterada. Nenhum Tenant Core foi implementado.

2) ESTADO ENCONTRADO ANTES DA ALTERAÇÃO
- Flyway já estava integrado (Prompt 0.1).
- `src/main/resources/db/migration/V1__baseline_schema.sql` existia como marker vazio (sem DDL).
- `application.properties` ainda estava orientado a H2 + `ddl-auto=create-drop` com Flyway habilitado.
  - Problema: baseline PostgreSQL real (quando existisse) iria falhar ao rodar em H2.

3) ESTRATÉGIA USADA PARA GERAR O BASELINE
Estratégia aplicada neste ambiente (fallback reproduzível sem Docker):
- Gerar o DDL a partir do metadata JPA/Hibernate com dialect PostgreSQL e salvar como script:
  - `mvn -q -Dtest=GenerateFlywayBaselineSchemaTest test`
  - Classe: `src/test/java/com/restaurante/tools/GenerateFlywayBaselineSchemaTest.java`
- Motivo do fallback:
  - Docker client existe, mas o socket `/var/run/docker.sock` não é acessível no ambiente atual (permission denied),
    inviabilizando `pg_dump` dentro de container/Testcontainers para esta execução local.

Estratégia preferida (recomendada para executar na máquina/CI com Docker/PG disponível):
- Criar DB temporário limpo `consuma_baseline`
- Subir app com `ddl-auto=create` para gerar schema
- Extrair schema com `pg_dump --schema-only --no-owner --no-privileges`
- Limpar dump (sem CREATE DATABASE/ALTER OWNER/GRANT) e consolidar em `V1__baseline_schema.sql`

4) BANCO/AMBIENTE USADO PARA EXTRAÇÃO
- Nesta execução: script gerado a partir do metadata JPA/Hibernate e validado com bootstrap do contexto Spring em H2 (MODE=PostgreSQL).
- Validação PostgreSQL real: preparada via Testcontainers em `it-postgres` (será SKIP sem Docker).

5) “COMANDO DE DUMP” UTILIZADO
- Não foi possível executar `pg_dump` nesta execução (restrição de acesso ao Docker socket).
- Comando recomendado (quando PostgreSQL em Docker estiver acessível):
  - `pg_dump --schema-only --no-owner --no-privileges -d consuma_baseline > src/main/resources/db/migration/V1__baseline_schema.sql`

6) ARQUIVOS ALTERADOS/CRIADOS
- Atualizado baseline:
  - `src/main/resources/db/migration/V1__baseline_schema.sql` (marker -> baseline real)
- Geração manual (reprodutível):
  - `src/test/java/com/restaurante/tools/GenerateFlywayBaselineSchemaTest.java`
- Profile de integração PostgreSQL/Testcontainers (Flyway + validate):
  - `src/test/resources/application-it-postgres.properties`
  - `src/test/java/com/restaurante/FlywayBaselinePostgresIT.java`
- Ajustes de profiles/testes:
  - `src/test/resources/application-test.properties` (Flyway desabilitado em H2)
  - `src/test/java/com/restaurante/config/TestBeansConfig.java` (ativo também em `it-postgres`)
- Configuração de runtime:
  - `src/main/resources/application.properties` (default PostgreSQL + Flyway + ddl-auto=validate)
  - `src/main/resources/application-dev-h2.properties` (profile legado H2 com Flyway off)
  - `docker-compose.prod.yml` (ddl-auto alterado para validate)
- Documentação:
  - `docs/tenant-core/00-preparacao-migrations-testes.md`
- (Mantido) Surefire excluindo E2E por padrão e excluindo tag manual:
  - `pom.xml`

7) AJUSTES EM CONFIGURAÇÕES FLYWAY/JPA
Objetivo: “Flyway cria / Hibernate valida”
- `application.properties` passou a assumir PostgreSQL por padrão + `spring.jpa.hibernate.ddl-auto=validate`
- Profile `dev-h2` criado para uso rápido (não recomendado para piloto), com:
  - `spring.flyway.enabled=false`
  - `ddl-auto=create-drop`
- `application-test.properties` (unit tests em H2) desabilita Flyway porque `V1` é PostgreSQL-native.
- `application-it-postgres.properties` habilita Flyway e força `ddl-auto=validate` para testes de integração.

8) VALIDAÇÃO EM BANCO LIMPO
- PostgreSQL real via Docker/Testcontainers:
  - Preparado com `FlywayBaselinePostgresIT` + profile `it-postgres`
  - Observação: sem Docker disponível neste ambiente, o teste é ignorado (disabledWithoutDocker).

9) RESULTADO DOS TESTES/BUILD
- `mvn -q -DskipTests compile` -> OK
- `mvn test -q` -> OK (unit tests H2; integração Postgres é SKIP sem Docker)
- Geração do baseline:
  - `mvn -q -Dtest=GenerateFlywayBaselineSchemaTest test` -> OK (gera/atualiza V1)

10) PROBLEMAS ENCONTRADOS + CORREÇÕES
- Docker socket inacessível (`permission denied` em `/var/run/docker.sock`):
  - Contornado com gerador via metadata JPA/Hibernate (reprodutível).
- Conflito potencial: baseline PostgreSQL vs perfil H2 (dev/test):
  - Resolvido separando profiles:
    - default: PostgreSQL + Flyway + validate
    - `dev-h2`: H2 + Flyway off
    - `test` (unit): H2 + Flyway off
    - `it-postgres`: Postgres/Testcontainers + Flyway on + validate

11) RISCOS RESTANTES
- O baseline gerado via metadata pode divergir de um dump real do PostgreSQL em detalhes (tipos, serial/identity, índices).
  - A validação final recomendada é gerar V1 via `pg_dump --schema-only` e comparar.
- Até o CI/ambiente garantir Docker disponível, o teste `it-postgres` pode ficar SKIP e não proteger contra regressões PostgreSQL-specific.

12) CHECKLIST DE ACEITAÇÃO (PROMPT 0.2)
[OK] `V1__baseline_schema.sql` deixou de ser marker vazio
[OK] `V1` representa schema atual completo (DDL + constraints + índices)
[PARCIAL] Flyway migrar banco PostgreSQL limpo (preparado via Testcontainers; execução depende de Docker disponível)
[OK] Hibernate valida schema no setup unitário; setup Postgres integration preparado
[OK] Testcontainers preparado para usar migrations reais (`it-postgres`)
[OK] `mvn test` executa com sucesso no ambiente atual
[OK] Nenhuma entidade de domínio foi alterada
[OK] Nenhuma regra de negócio foi alterada
[OK] Documentação atualizada com baseline real
[OK] Projeto pronto para Prompt 1 (Tenant Core) iniciar em V2

13) PRÓXIMO PROMPT RECOMENDADO
Prompt 1 — Fundação do Domínio SaaS:
- Criar `V2__create_tenant_core.sql` (Tenant/Plano/Subscricao/TenantUser/TenantLimiteOverride)
- Seeds do Plano PILOTO via migration/seed controlado
- Testes de persistência e isolamento iniciais (Postgres/Testcontainers)

