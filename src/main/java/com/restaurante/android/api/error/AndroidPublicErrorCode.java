package com.restaurante.android.api.error;

/** Runtime foundation codes. Endpoint-specific contract codes remain reserved until their phases. */
public enum AndroidPublicErrorCode {
    INVALID_REQUEST,
    INTERNAL_ERROR,
    SERVICE_UNAVAILABLE
}
